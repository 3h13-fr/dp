# Copy to .env.test and adjust. Used by: pnpm run db:test:reset
# DATABASE_URL must point to a dedicated test database (e.g. mobility_test).

DATABASE_URL="postgresql://mobility:mobility@localhost:5432/mobility_test"

# Note: For e2e tests (Playwright), also configure apps/web/.env.local:
# - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_... (required for payment tests)
# - NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.eyJ... (optional, for map features)

import { test } from '@playwright/test';

test.describe('Review: leave review after service', () => {
  test.skip('UI for leaving a review not yet implemented', () => {
    // When implemented: complete a booking as guest → open booking detail →
    // see "Leave a review" → submit rating + comment → verify review appears.
  });
});

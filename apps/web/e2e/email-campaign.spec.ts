import { test } from '@playwright/test';

test.describe('Email campaign', () => {
  test.skip('Campaign feature not yet in app: create → preview → send to test segment → verify log', () => {
    // When implemented: admin creates campaign → preview → send to test segment → check logs.
  });
});
